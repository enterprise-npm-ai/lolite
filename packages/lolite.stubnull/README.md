# lolite.stubnull

### stubNull()
Returns the primitive value null.
```javascript
const stubNull = require("lolite.stubnull")
console.log(stubNull()) // null
```

This utility is part of the [LoLite](https://github.com/enterprise-npm-ai/lolite) utility suite.